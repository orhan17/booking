// Package adapters implements the ports: operator clients, repositories, HTTP
// handlers, and other driving/driven adapters.
//
// It may import ports and domain types. It is never imported by app or domain.
package adapters
